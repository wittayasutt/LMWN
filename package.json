{
	"name": "root",
	"private": true,
	"devDependencies": {
		"lerna": "^4.0.0"
	},
	"scripts": {
		"dev": "npx lerna run dev --parallel",
		"test": "npx lerna run test --parallel"
	},
	"workspaces": {
		"packages": [
			"packages/**"
		]
	}
}
